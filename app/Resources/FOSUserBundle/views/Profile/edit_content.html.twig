{% trans_default_domain 'FOSUserBundle' %}

<div style="max-width: 70%">
{{ form_start(form, { 'action': path('fos_user_profile_edit'), 'attr': { 'class': 'fos_user_profile_edit' } }) }}
    {{ form_widget(form) }}
    <div>
        <button type="submit" class="btn btn-primary"> <span class="glyphicon glyphicon-plus-move"  ></span> {{ 'profile.edit.submit'|trans }}</button></div>

</div>

{{ form_end(form) }}
